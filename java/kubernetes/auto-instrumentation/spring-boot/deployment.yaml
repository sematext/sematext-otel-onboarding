apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-spring-k8s-auto
  labels:
    app: java-spring-k8s-auto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-spring-k8s-auto
  template:
    metadata:
      labels:
        app: java-spring-k8s-auto
    spec:
      # Init container downloads the OTel Java agent into a shared volume
      initContainers:
        - name: otel-agent
          image: alpine:3
          command: ['wget', '-O', '/otel/opentelemetry-javaagent.jar', 'https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/latest/download/opentelemetry-javaagent.jar']
          volumeMounts:
            - name: otel-agent
              mountPath: /otel

      containers:
      - name: java-spring-k8s-auto
        image: java-spring-k8s-auto:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http

        # Mount the shared volume containing the OTel agent
        volumeMounts:
          - name: otel-agent
            mountPath: /otel

        env:
        # Inject the OTel Java agent via JAVA_TOOL_OPTIONS
        - name: JAVA_TOOL_OPTIONS
          value: "-javaagent:/otel/opentelemetry-javaagent.jar"

        # OpenTelemetry configuration
        - name: OTEL_SERVICE_NAME
          value: "java-spring-k8s-auto"
        - name: OTEL_SERVICE_VERSION
          value: "1.0.0"
        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: "http/protobuf"
        - name: OTEL_EXPORTER_OTLP_TRACES_ENDPOINT
          value: "http://st-agent-sematext-agent.sematext.svc.cluster.local:4338/v1/traces"
        - name: OTEL_EXPORTER_OTLP_METRICS_ENDPOINT
          value: "http://st-agent-sematext-agent.sematext.svc.cluster.local:4318/v1/metrics"
        - name: OTEL_EXPORTER_OTLP_LOGS_ENDPOINT
          value: "http://st-agent-sematext-agent.sematext.svc.cluster.local:4328/v1/logs"
        - name: OTEL_LOGS_EXPORTER
          value: "otlp"

        # Capture Kubernetes metadata
        - name: K8S_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "service.namespace=$(K8S_NAMESPACE),k8s.pod.name=$(K8S_POD_NAME)"

        # Health checks
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 50
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        # Resource limits
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"

        # Security context
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
              - ALL
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false

      volumes:
        - name: otel-agent
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: java-spring-k8s-auto
  labels:
    app: java-spring-k8s-auto
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: java-spring-k8s-auto
